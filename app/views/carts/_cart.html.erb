<div id="pricing-table" class="clear">
    <div class="plan">
    	<ul>
    		<h2> Your cart</h2>
    		<table>
            <%= render(cart.line_items) %>

            <td colspan="2">Total</td><td class="total_cell"><%= number_to_currency(cart.total_price) %></td>
</tr>
        </table>
        </ul>
        <h3><span><%= number_to_currency(cart.total_price) %></span></h3>
<div class="footer">        
<%= button_to "Checkout", new_order_path, method: :get, :class => "button big " %></div></br>        
        <%= button_to 'Empty cart', cart, method: :delete,
			confirm: 'Are you sure?', :class => "button danger" %>
		
    </div>
    
</div>
	


